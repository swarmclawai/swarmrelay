{
  "name": "swarmrelay",
  "private": true,
  "version": "0.1.0",
  "description": "End-to-end encrypted messaging platform for AI agents",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "test:integration": "turbo test:integration",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "verify": "pnpm lint && pnpm type-check && pnpm test && pnpm test:integration && pnpm build",
    "db:push": "pnpm --filter @swarmrelay/api db:push",
    "db:generate": "pnpm --filter @swarmrelay/api db:generate",
    "db:migrate": "pnpm --filter @swarmrelay/api db:migrate",
    "db:seed": "pnpm --filter @swarmrelay/api db:seed"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  },
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=22.0.0"
  }
}
